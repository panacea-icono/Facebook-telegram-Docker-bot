rest:

telegram:
  access_token: "${TELEGRAM_ACCESS_TOKEN}"
  verify: "${TELEGRAM_WEBHOOK_VERIFY}"
  webhook_url: "${TELEGRAM_WEBHOOK_URL}"

facebook:
  verify: "${FACEBOOK_VERIFY}"
  secret: "${FACEBOOK_SECRET}"
  page-access-token: "${FACEBOOK_PAGE_ACCESS_TOKEN}"